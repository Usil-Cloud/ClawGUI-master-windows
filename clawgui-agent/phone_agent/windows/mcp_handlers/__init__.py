# Notes: docs/phone_agent/windows/mcp_handlers/_index.md
"""Handler modules for the five top-level MCP tools.

Each module owns one tool's full implementation. `mcp_server.py` decorators
are thin wrappers that delegate here. See `docs/features/mcp_server/design.md`
for the composition rules.
"""
